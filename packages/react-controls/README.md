# @veit/react-controls

Small accessible controls: `VeitSwitch`, `VeitSettingsToggleRow`.

**Peer dependency:** `react`.

**Theming:** Tailwind semantic classes only; host app must provide compatible CSS variables / utilities.
